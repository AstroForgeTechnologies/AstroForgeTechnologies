---
import { getCollection } from "astro:content";
import Posts from "@layouts/Posts.astro";
import getSortedPosts from "../../utils/getSortedPosts";

const posts = getSortedPosts(await getCollection("development"));
---

<Posts {posts} />
